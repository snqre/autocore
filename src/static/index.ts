import { Color } from "three";

export namespace Static {
    export const DEFAULT_X: number = 0;
    export const DEFAULT_Y: number = 0;
    export const DEFAULT_W: number = 100;
    export const DEFAULT_H: number = 100;
    export const DEFAULT_COLOR: Color = new Color(0x202020);
}